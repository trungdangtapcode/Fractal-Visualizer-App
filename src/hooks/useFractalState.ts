// Placeholder hooks. To be expanded based on fractal type.
export function useSharedFractalState() {
    // In Clean Architecture, we separate UI states (pan, zoom, colors, iterations)
    // from the business logic layer that executes the WebGL/Canvas rendering.
}